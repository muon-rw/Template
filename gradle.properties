# Important Notes:
# Every field you add must be added to buildSrc/src/main/groovy/multiloader-common.gradle expandProps map.

# Project
version=0.1.0
group=dev.muon.template
java_version=25

# Common
minecraft_version=26.1.2
mod_name=Template
mod_author=Muon
mod_id=template
# This is the default license for mods generated by this, feel free to change to something more permissive
license=ARR
credits=
description=Template
minecraft_version_range=[26.1.2, 26.2)
## This is the version of minecraft that the 'common' project uses, you can find a list of all versions here
## https://projects.neoforged.net/neoforged/neoform
neo_form_version=26.1.2-1

# Fabric, see https://fabricmc.net/develop/ for new versions
# Fabric can not bump until FzzyConfig updates past 0.7.6
fabric_version=0.155.2+26.1.2
fabric_loader_version=0.19.3

# NeoForge, see https://projects.neoforged.net/neoforged/neoforge for new versions
neoforge_version=26.1.2.84
neoforge_loader_version_range=[4,)

# Gradle
org.gradle.jvmargs=-Xmx6G
org.gradle.daemon=false

# FzzyConfig
fzzy_config=0.7.6+26.1

# Mixin
fabric_mixin_version=0.17.3+mixin.0.8.7
mixin_extras_version=0.5.4
mixin_squared_version=0.3.7-beta.1

modmenu_version=18.0.0-alpha.8

# Publishing (fill in with your own project IDs or leave blank to skip that platform)
curseforge_id=
modrinth_id=
# GitHub: used for source/issues links in mod metadata (fabric.mod.json, neoforge.mods.toml).
# Not wired up to mod-publish-plugin.
# e.g. github_owner=muon-rw | github_repo=Template | links to https://github.com/muon-rw/Template
github_owner=
github_repo=

# Modpack dev-jar destinations (optional). Path to your modpack's `mods/` folder.
# Relative paths resolve against the user home dir (so the same value works on
# Windows and macOS when CurseForge/etc. lives under your home). Absolute paths
# are used as-is. When set, registers sendToModpackFab / sendToModpackNeo, which
# copy the built jar with its version replaced by `dev` into the target folder.
fabric_modpack_path=
neoforge_modpack_path=